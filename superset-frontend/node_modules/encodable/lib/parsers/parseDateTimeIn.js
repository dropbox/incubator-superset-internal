"use strict";

exports.__esModule = true;
exports.default = parseDateTimeIn;

var _DateTime = require("../typeGuards/DateTime");

var _parseDateTime = _interopRequireDefault(require("./parseDateTime"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * Only parse elements that are DateTime to Date.
 * Leave the rest alone.
 * @param array
 */
function parseDateTimeIn(array) {
  return array.map(d => !(d instanceof Date) && (0, _DateTime.isDateTime)(d) ? (0, _parseDateTime.default)(d) : d);
}