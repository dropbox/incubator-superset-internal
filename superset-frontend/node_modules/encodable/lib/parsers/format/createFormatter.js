"use strict";

exports.__esModule = true;
exports.default = createFormatter;

var _fallbackFormatter = _interopRequireDefault(require("./fallbackFormatter"));

var _Encodable = _interopRequireDefault(require("../../options/Encodable"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function createFormatter({
  formatType,
  format,
  formatInLocalTime
}) {
  if (formatType === 'time') {
    const formatter = _Encodable.default.resolveTimeFormat({
      format,
      formatInLocalTime
    });

    return value => formatter(value);
  }

  if (formatType === 'number' || typeof format !== 'undefined' && format.length > 0) {
    const formatter = _Encodable.default.resolveNumberFormat(format);

    return value => formatter(value);
  }

  return _fallbackFormatter.default;
}