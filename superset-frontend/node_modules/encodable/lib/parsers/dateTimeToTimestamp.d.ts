import { DateTime } from '../types';
/**
 * Internal Object for defining datetime expressions.
 * This is an expression version of DateTime.
 * If both month and quarter are provided, month has higher precedence.
 * `day` cannot be combined with other date.
 */
interface DateTimeExpr {
    year?: string;
    quarter?: string;
    month?: string;
    date?: string;
    day?: string;
    hours?: string;
    minutes?: string;
    seconds?: string;
    milliseconds?: string;
    utc?: boolean;
}
export declare function normalizeQuarter(q: number | string): number;
export declare function normalizeMonth(m: string | number): number;
export declare function normalizeDay(d: string | number): number;
/**
 * @param d the date.
 * @param normalize whether to normalize quarter, month, day. This should probably be true if d is a DateTime.
 * @returns array of date time parts [year, month, day, hours, minutes, seconds, milliseconds]
 */
export declare function dateTimeParts(d: DateTime | DateTimeExpr, normalize: boolean): (string | number)[];
/**
 * @param d the date time.
 * @returns the timestamp.
 */
export default function dateTimeToTimestamp(d: DateTime): number;
export {};
//# sourceMappingURL=dateTimeToTimestamp.d.ts.map