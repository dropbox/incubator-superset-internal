import { DateTime } from '../types';
/**
 * Only parse elements that are DateTime to Date.
 * Leave the rest alone.
 * @param array
 */
export default function parseDateTimeIn<T>(array: (DateTime | T)[]): (Date | T)[];
//# sourceMappingURL=parseDateTimeIn.d.ts.map