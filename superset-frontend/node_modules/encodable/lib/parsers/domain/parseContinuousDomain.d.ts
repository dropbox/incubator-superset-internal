import { ScaleType, ContinuousInput } from '../../types';
/**
 * Convert each element in the array into
 * - Date (for time scales)
 * - number (for other continuous scales)
 * @param domain
 * @param scaleType
 */
export default function parseContinuousDomain<T extends ContinuousInput | null | undefined>(domain: T[], scaleType: ScaleType): Exclude<T, string | number | boolean>[] | Exclude<T, string | boolean>[];
//# sourceMappingURL=parseContinuousDomain.d.ts.map