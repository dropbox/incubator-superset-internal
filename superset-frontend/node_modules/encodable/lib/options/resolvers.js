"use strict";

exports.__esModule = true;
exports.defaultCategoricalColorScaleResolver = exports.defaultColorSchemeResolver = exports.defaultTimeFormatResolver = exports.defaultNumberFormatResolver = void 0;

var _color = require("@encodable/color");

var _format = require("@encodable/format");

const defaultNumberFormatResolver = _format.getNumberFormatter;
exports.defaultNumberFormatResolver = defaultNumberFormatResolver;

const defaultTimeFormatResolver = ({
  format,
  formatInLocalTime = false
} = {}) => {
  const formatString = formatInLocalTime ? (0, _format.addPrefix)(_format.LOCAL_TIME_PREFIX, format != null ? format : (0, _format.getTimeFormatterRegistry)().getDefaultKey()) : format;
  return (0, _format.getTimeFormatter)(formatString);
};

exports.defaultTimeFormatResolver = defaultTimeFormatResolver;

const defaultColorSchemeResolver = ({
  name,
  type = 'categorical'
} = {}) => (0, _color.getColorSchemeRegistry)()[type].get(name);

exports.defaultColorSchemeResolver = defaultColorSchemeResolver;

const defaultCategoricalColorScaleResolver = ({
  name,
  namespace
} = {}) => (0, _color.getScale)(name, namespace);

exports.defaultCategoricalColorScaleResolver = defaultCategoricalColorScaleResolver;