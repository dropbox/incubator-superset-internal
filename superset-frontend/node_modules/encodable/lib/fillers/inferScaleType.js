"use strict";

exports.__esModule = true;
exports.default = inferScaleType;

var _types = require("../types");

/**
 * Sometimes scale type is not specified but can be inferred
 * from other fields.
 * See https://vega.github.io/vega-lite/docs/scale.html
 * @param channelType type of the channel
 * @param fieldType type of the field
 * @param bin is value binned
 */
function inferScaleType(channelType, fieldType, bin = false) {
  if (fieldType === 'nominal' || fieldType === 'ordinal') {
    switch (channelType) {
      // For positional (x and y) ordinal and ordinal fields,
      // "point" is the default scale type for all marks
      // except bar and rect marks, which use "band" scales.
      // https://vega.github.io/vega-lite/docs/scale.html
      case 'XBand':
      case 'YBand':
        return _types.ScaleType.BAND;

      case 'X':
      case 'Y':
      case 'Numeric':
        return _types.ScaleType.POINT;

      case 'Color':
      case 'Category':
        return _types.ScaleType.ORDINAL;

      default:
    }
  } else if (fieldType === 'quantitative') {
    switch (channelType) {
      case 'XBand':
      case 'YBand':
      case 'X':
      case 'Y':
      case 'Numeric':
        return _types.ScaleType.LINEAR;

      case 'Color':
        return bin ? _types.ScaleType.BIN_ORDINAL : _types.ScaleType.LINEAR;

      default:
    }
  } else if (fieldType === 'temporal') {
    switch (channelType) {
      case 'XBand':
      case 'YBand':
      case 'X':
      case 'Y':
      case 'Numeric':
        return _types.ScaleType.UTC;

      case 'Color':
        return _types.ScaleType.LINEAR;

      default:
    }
  }

  return undefined;
}