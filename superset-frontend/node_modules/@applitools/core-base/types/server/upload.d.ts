/// <reference types="node" />
/// <reference types="node" />
import { type Logger } from '@applitools/logger';
import { type Proxy } from '@applitools/req';
export declare type Upload = (options: {
    name: string;
    resource: Buffer | URL | string;
    gzip?: boolean;
}) => Promise<string>;
export declare function makeUpload({ config, logger }: {
    config: {
        uploadUrl: string;
        proxy?: Proxy;
    };
    logger?: Logger;
}): Upload;
