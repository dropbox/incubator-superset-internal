const chunkify = require('./src/chunkify');
const pollify = require('./src/pollify');
const poll = require('./src/poll');
const absolutizeUrl = require('./src/browser/absolutizeUrl');
const isInlineFrame = require('./src/browser/isInlineFrame');
const isAccessibleFrame = require('./src/browser/isAccessibleFrame');

module.exports = {
  chunkify,
  pollify,
  poll,
  absolutizeUrl,
  isInlineFrame,
  isAccessibleFrame,
};
