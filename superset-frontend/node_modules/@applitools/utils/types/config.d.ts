export declare function getConfig({ paths, params, traverse, strict, logger, }?: {
    paths?: string[];
    params?: string[];
    traverse?: false | {
        stopDir: string;
    };
    strict?: boolean;
    logger?: any;
}): Record<string, any>;
