"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = labelSize;
function labelSize(_ref) {
  var scale = _ref.scale,
      domain = _ref.domain,
      _ref$steps = _ref.steps,
      steps = _ref$steps === undefined ? 5 : _ref$steps,
      labelFormat = _ref.labelFormat;

  var start = domain[0];
  var end = domain[domain.length - 1];
  var step = (end - start) / (steps - 1);
  var data = new Array(steps).fill(1).reduce(function (acc, cur, i) {
    acc.push(start + i * step);
    return acc;
  }, []);
  return data.map(function (d, i) {
    return {
      extent: [],
      text: "" + labelFormat(d, i),
      value: scale(d)
    };
  });
}