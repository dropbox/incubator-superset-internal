import { TooltipLinkList } from '@storybook/components';
import { ComponentProps } from 'react';
export declare type Padding = {
    name: string;
    value: string;
};
export declare type PaddingsParameter = {
    default?: string;
    disable?: boolean;
    values: Padding[];
};
export declare type PaddingWithDefault = Padding & {
    default?: boolean;
};
export declare type Item = ComponentProps<typeof TooltipLinkList>['links'][0];
export declare type GlobalState = {
    name?: string;
    selected?: string;
};
