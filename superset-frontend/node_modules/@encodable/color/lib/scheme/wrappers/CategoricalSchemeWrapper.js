"use strict";

exports.__esModule = true;
exports.default = void 0;

var _d3Scale = require("d3-scale");

var _SchemeWrapper = _interopRequireDefault(require("./SchemeWrapper"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

class CategoricalSchemeWrapper extends _SchemeWrapper.default {
  get colors() {
    return this.scheme.colors;
  }

  getColors(numColors) {
    const {
      length
    } = this.scheme.colors;

    if (typeof numColors === 'undefined' || numColors === length) {
      return this.scheme.colors;
    }

    if (numColors < length) {
      return this.scheme.colors.slice(0, numColors);
    }

    const times = Math.ceil(numColors / length);
    let output = this.scheme.colors.slice();

    for (let i = 0; i < times; i += 1) {
      output = output.concat(this.scheme.colors);
    }

    return output.slice(0, numColors);
  }

  createScaleOrdinal() {
    return (0, _d3Scale.scaleOrdinal)(this.scheme.colors);
  }

}

exports.default = CategoricalSchemeWrapper;