import { RegistryConfig } from '@encodable/registry';
import ColorNamespace from './ColorNamespace';
export declare const DEFAULT_NAMESPACE = "DEFAULT_NAMESPACE";
export default class ColorNamespaceRegistry {
    private readonly namespaceStates;
    private readonly namespaceInstances;
    constructor({ name, globalId, ...rest }?: RegistryConfig);
    getDefaultNamespace(): string;
    setDefaultNamespace(namespace: string): this;
    has(namespace: string): boolean;
    get(namespace?: string): ColorNamespace;
    keys(): string[];
    clear(): this;
    remove(namespace: string): this;
}
//# sourceMappingURL=ColorNamespaceRegistry.d.ts.map