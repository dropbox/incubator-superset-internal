import { TimeLocaleDefinition } from 'd3-time-format';
import { TimeFormatterMetadata } from '../../types';
interface Config extends TimeFormatterMetadata {
    format: string;
    locale?: TimeLocaleDefinition;
}
export default function createD3TimeFormatter({ format, locale, useLocalTime, id, label, description, }: Config): import("../../types").TimeFormatter;
export {};
//# sourceMappingURL=createD3TimeFormatter.d.ts.map