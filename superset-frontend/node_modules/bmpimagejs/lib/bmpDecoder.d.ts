import { ImageRGBA } from './imageRGBA';
export declare class BmpDecoder {
    private _bytes;
    decode(buffer: ArrayBuffer): ImageRGBA;
    private startDecoding();
    private readonly BMP_FILE_HEADER_SIZE;
    private bmpFileHeaderParse(data);
    private readonly BITMAPCOREHEADER;
    private bmpInfoHeaderParse(data, header);
    private bmpDecode(data, header);
    private bmpDecode_rgb32(data, header);
    private bmpDecode_rgb24(data, header);
    private bmpDecode_rgb16(data, header);
    private bmpDecode_rgb(data, header);
    private bmpDecode_rle8(data, header);
    private bmpDecode_rle4(data, header);
}
